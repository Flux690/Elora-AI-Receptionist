{
  "name": "deskroute",
  "version": "1.0.8",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently --names \"api,agent,ui\" --prefix-colors \"cyan,yellow,magenta\" --kill-others \"pnpm --dir backend exec tsx watch src/index.ts\" \"pnpm --dir backend exec tsx src/agent/worker.ts dev\" \"pnpm -F frontend dev\"",
    "dev:backend": "pnpm -F backend dev",
    "dev:agent": "pnpm -F backend agent",
    "dev:frontend": "pnpm -F frontend dev",
    "build": "pnpm -F frontend build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -F frontend lint",
    "test": "pnpm -F backend test"
  },
  "devDependencies": {
    "concurrently": "9.2.1"
  }
}
